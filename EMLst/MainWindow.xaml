<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EMLst.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="EM Dispatch Adapter">

    <Grid Padding="10" VerticalAlignment="Top" Width="400" Height="200">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- TextBox for Token -->
        <TextBox x:Name="TokenTextBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Token" Margin="0,0,0,10"/>

        <!-- Request Token Button -->
        <Button x:Name="RequestTokenButton" Grid.Row="1" Grid.Column="1" Content="Request Token" Margin="5,0,0,10" Click="RequestTokenButton_Click"/>

        <!-- Save Token Button -->
        <Button x:Name="SaveTokenButton" Grid.Row="1" Grid.Column="2" Content="Save Token" Margin="5,0,0,10" Click="SaveTokenButton_Click"/>

        <!-- Display File Path Button -->
        <TextBlock x:Name="FilePathTextBlock" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="No file selected" VerticalAlignment="Center" Margin="0,0,10,10"/>

        <!-- Choose File Button -->
        <Button x:Name="ChooseFileButton" Grid.Row="2" Grid.Column="2" Content="Choose Logfile Folder" Click="ChooseFileButton_Click"/>
        
        <TextBlock x:Name="FileConfigTextBlock" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Text="No file selected" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <Button x:Name="ChooseConfigButton" Grid.Row="3" Grid.Column="2" Content="Choose Config File" Click="ChooseConfigButton_Click"/>
    </Grid>
</Window>
